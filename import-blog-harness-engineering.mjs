/**
 * import-blog-harness-engineering.mjs
 * ---------------------------------------------------------------
 * Inserts (or updates) the Harness Engineering post.
 *
 *   node import-blog-harness-engineering.mjs --dry
 *   node import-blog-harness-engineering.mjs
 *
 * Upserts on `slug`, so re-running edits the existing row rather
 * than creating a duplicate. Only columns that actually exist on
 * the table are written -- the schema is read at run time, so a
 * missing optional column is reported, not fatal.
 */
import pg from 'pg';
import { readFileSync } from 'node:fs';
const { Client } = pg;

try {
  for (const line of readFileSync('.env.local', 'utf8').split('\n')) {
    const m = line.match(/^\s*([A-Z0-9_]+)\s*=\s*(.*)\s*$/);
    if (m && !process.env[m[1]]) process.env[m[1]] = m[2].replace(/^["']|["']$/g, '');
  }
} catch {}

const TABLE = 'blogs';
const DRY_RUN = process.argv.includes('--dry');
const url = process.env.DATABASE_URL;
if (!url) { console.error('DATABASE_URL is not set.'); process.exit(1); }

const blog = {
  "slug": "harness-engineering-ai-coding-agents",
  "title": "Harness Engineering: AI Coding Agents for Reliable Software",
  "description": "Learn how Harness Engineering helps teams build reliable software with AI coding agents through context, testing, guardrails, automation, and human oversight.",
  "content": "<p>AI coding agents are moving beyond single code suggestions and beginning to perform larger development tasks &mdash; navigating a repository, changing multiple files, running commands, and iterating on their own output. That shift raises a different question for engineering teams: not whether the model can write code, but whether the surrounding workflow is designed for it to do so reliably.</p>\n<p>This guide explains harness engineering: the practice of designing the context, rules, tools, validation, and controls that let AI coding agents contribute to real software projects.</p>\n<h2>What Is Harness Engineering?</h2>\n<p><strong>Harness Engineering is a software engineering approach for creating the right environment, rules, tools, and validation processes for AI coding agents to work reliably on real software projects.</strong></p>\n<p>Rather than treating AI as a tool that simply generates code from prompts, harness engineering focuses on the engineering system built around the AI.</p>\n<p>An AI coding agent may understand requirements, navigate a repository, modify multiple files, run commands, and fix implementation issues. But producing reliable software requires more than technically valid code. The agent also needs relevant project context, clear engineering expectations, appropriate development tools, and reliable ways to verify its changes.</p>\n<p>This makes harness engineering different from basic AI-assisted coding.</p>\n<p>In a traditional workflow, a developer may ask an AI tool to generate or modify a piece of code. With harness engineering, the development workflow is designed to help the AI operate within the project's architecture, conventions, testing practices, and operational boundaries. This is the difference between a prompt and a system, and it is where <a href=\"/generative-ai-development-company\">AI software development</a> starts to resemble ordinary engineering discipline.</p>\n<p>The central idea is simple: the quality of AI-assisted software development depends not only on the capability of the AI model, but also on the engineering system in which it operates.</p>\n<p>A well-designed harness connects AI capabilities with established software engineering practices. It gives agents the information needed to understand the project, the guidance needed to make appropriate implementation decisions, and the validation needed to determine whether their changes actually work.</p>\n<p>Harness engineering is therefore not about giving AI maximum freedom. It is about creating controlled autonomy &mdash; allowing AI coding agents to perform meaningful development work while keeping quality, reliability, security, and important engineering decisions under appropriate control.</p>\n<p>The key question shifts from &ldquo;How can AI write better code?&rdquo; to &ldquo;How should we design software development so AI can build and change software reliably?&rdquo;</p>\n<h2>Why AI Coding Agents Need an Engineering Harness</h2>\n<p>As AI coding agents take on larger development tasks, software teams face a different engineering challenge: how to ensure that AI-generated changes meet the project's quality, architectural, and operational requirements.</p>\n<p>Software projects contain business rules, architectural decisions, dependencies, technical constraints, and quality standards. An implementation can compile and appear correct while still introducing a regression, violating an architectural boundary, adding unnecessary complexity, or missing an important requirement.</p>\n<p>The larger the task an AI agent performs, the more important these risks become.</p>\n<p>A harness provides the engineering structure needed to manage those risks. Instead of evaluating an AI coding agent only by whether it completed a requested task, the development process also evaluates whether the resulting change is correct, compatible, maintainable, and acceptable for the project.</p>\n<p>A key principle is: an AI agent can implement a change, but the engineering system should determine whether that change is acceptable.</p>\n<p>This creates a model of managed autonomy. AI coding agents can handle meaningful development work without making software quality dependent entirely on their own judgment. The surrounding engineering process establishes expectations and evaluates outcomes. When a change does not meet those expectations, the agent or developer can investigate, revise, and validate it again.</p>\n<p>The result is a more reliable development cycle: AI performs the work, the change is evaluated, issues are addressed, and the result is validated.</p>\n<h2>How AI Changes the Role of Software Engineers</h2>\n<p>AI coding agents are changing software engineering not only by accelerating implementation, but by shifting where engineers spend their time and judgment.</p>\n<p>Traditional development often requires engineers to write code, debug issues, implement repetitive logic, and handle many of the smaller decisions involved in delivering a feature. As AI takes on more of these activities, engineers can spend more time on problems that require broader technical and product understanding. This changes the engineer's role from primarily producing implementation to increasingly directing and evaluating it &mdash; a shift that is reshaping how <a href=\"/custom-software-development-company\">AI software engineering</a> teams are organised.</p>\n<ul><li>which parts of a system are suitable for AI-assisted development</li><li>which architectural decisions should remain human-owned</li><li>what engineering standards must be preserved</li><li>how AI-generated changes should be evaluated</li><li>when a task requires human intervention</li></ul>\n<p>This does not make engineering judgment less important. In many cases it makes that judgment more important, because AI can produce multiple implementation options quickly while an engineer must determine which approach best fits the application's architecture, requirements, maintainability, and long-term direction.</p>\n<p>Harness engineering reinforces this shift by making engineers responsible for designing the conditions in which AI development takes place. The resulting workflow is less about who writes every line of code and more about how effectively a team turns requirements into reliable software.</p>\n<h2>What Does an AI Engineering Harness Include?</h2>\n<p>An AI engineering harness is not a single tool or platform. It is a connected set of engineering practices, project information, tools, and controls that support AI coding agents within a software development workflow.</p>\n<p>A practical harness typically includes:</p>\n<ul><li><strong>Project context</strong> &mdash; information that helps the agent understand the codebase and its structure.</li><li><strong>Engineering instructions</strong> &mdash; project-specific rules and expectations for implementation.</li><li><strong>Validation mechanisms</strong> &mdash; tests and automated checks that evaluate changes.</li><li><strong>Development tools</strong> &mdash; controlled access to the commands and systems required for development tasks.</li><li><strong>Feedback signals</strong> &mdash; information from test results, errors, builds, and other development outcomes.</li><li><strong>Security controls</strong> &mdash; boundaries around access, permissions, sensitive systems, and risky operations.</li><li><strong>Human decision points</strong> &mdash; situations where engineering judgment or approval is required.</li></ul>\n<p>These components work together as a connected development workflow: understand the project, perform the work, validate the change, receive feedback, refine the result, and review when required.</p>\n<h2>Designing Repository Context for AI Coding Agents</h2>\n<p>For an AI coding agent, the repository is more than a collection of source files. It is one of the primary places where the information needed to make implementation decisions can be discovered. A well-structured repository should make important engineering knowledge easy to find &mdash; which is as true for <a href=\"/custom-software-development-company\">custom software development</a> teams as it is for the agents working alongside them.</p>\n<h3>Create a Clear Repository Map</h3>\n<p>AI coding agents should be able to identify the major areas of a project without unnecessary exploration. Clear organization can separate:</p>\n<ul><li>application code</li><li>shared components and libraries</li><li>APIs and services</li><li>database and data-access logic</li><li>configuration</li><li>tests</li><li>infrastructure</li><li>documentation</li><li>development scripts and utilities</li></ul>\n<p>The exact structure depends on the technology and architecture, but the goal is consistent: the location and responsibility of important code should be understandable.</p>\n<h3>Make Architectural Decisions Discoverable</h3>\n<p>Repository context should also expose information that is not obvious from individual files: application architecture, module responsibilities, service boundaries, integration patterns, important dependencies, data flows, and known technical constraints. When this information is difficult to discover, an AI coding agent may make locally reasonable changes that conflict with broader system design.</p>\n<h3>Keep Project Conventions Close to the Code</h3>\n<p>Naming conventions, file organization, error-handling patterns, dependency practices, and testing approaches should be discoverable within the project wherever practical. Documentation also needs to remain aligned with the current implementation &mdash; outdated documentation can be as problematic as missing documentation, because it may lead development decisions in the wrong direction.</p>\n<p>A useful principle: if important engineering knowledge exists only outside the repository, it is harder to make that knowledge consistently available during AI-assisted development.</p>\n<h2>Engineering Instructions and Rules for AI Coding Agents</h2>\n<p>Repository context tells an AI coding agent where to find information. Engineering instructions define how the team expects development work to be performed.</p>\n<p>These instructions provide practical guidance for decisions that repeatedly matter across a project. They can cover:</p>\n<ul><li>preferred coding patterns</li><li>naming and file-organization conventions</li><li>architectural boundaries</li><li>dependency policies</li><li>error-handling practices</li><li>testing expectations</li><li>development and validation commands</li><li>rules for modifying sensitive areas</li><li>situations that require human review</li></ul>\n<p>The goal is not to create a large rulebook. Instructions are most effective when they are specific, actionable, and relevant to the work being performed. Instead of telling an agent to write &ldquo;clean code&rdquo;, a project can define concrete expectations around module boundaries, dependency usage, API behaviour, or error handling.</p>\n<p>Instructions can also operate at different levels. General project rules may apply across the codebase, while additional guidance can apply to a particular service, module, or workflow. These rules should evolve with the software: changes to architecture, frameworks, dependencies, or development practices may require corresponding updates to the instruction layer.</p>\n<h2>Testing as the Verification Layer</h2>\n<p>For AI coding agents, completing an implementation is only part of the development process. The resulting change must also be checked against the project's expected behaviour. Testing provides an objective way to evaluate AI-generated changes, and it is where <a href=\"/quality-assurance-and-testing-services\">software testing services</a> earn their place in an agentic workflow.</p>\n<h3>Tests as Executable Specifications</h3>\n<p>Well-designed tests capture expected application behaviour in a form that can be executed repeatedly. This matters especially when an AI coding agent changes existing functionality: tests can confirm that intended behaviour works while helping detect unintended changes to areas that were not part of the task.</p>\n<h3>Layer Validation Around the Change</h3>\n<ul><li><strong>Unit tests</strong> verify individual functions or components.</li><li><strong>Integration tests</strong> verify interactions between system components.</li><li><strong>End-to-end tests</strong> cover important application workflows.</li><li><strong>Type checks and linting</strong> identify structural and code-quality issues.</li><li><strong>Build checks</strong> confirm that the application can be successfully compiled or packaged.</li></ul>\n<p>The right combination depends on the project. The objective is not to maximise the number of checks, but to cover the behaviours and failure modes that matter.</p>\n<h3>Practical Example: Adding a Payment Method</h3>\n<p>Suppose an AI coding agent is asked to add a new payment method to an e-commerce application. The change may involve the payment service, API layer, database models, and checkout interface. The task should not be considered complete simply because those files have been modified. Automated checks can verify that the new payment flow works, existing payment methods remain functional, invalid transactions are handled correctly, and the application still builds.</p>\n<p>If an existing payment test fails after the change, that failure gives the agent and engineer a concrete signal to investigate and correct the implementation.</p>\n<h3>Make Completion Measurable</h3>\n<p>For an AI coding agent, &ldquo;done&rdquo; should mean more than code has been generated or modified. A stronger definition of completion is based on acceptance criteria and successful validation.</p>\n<h2>Tools, Permissions, and Controlled Autonomy</h2>\n<p>AI coding agents become more useful when they can interact with the tools required to complete development tasks. However, unrestricted access to every system can introduce unnecessary risk. Harness engineering therefore treats tool access and permissions as part of the development design &mdash; a familiar constraint in <a href=\"/enterprise-application-development-company\">enterprise software development</a>.</p>\n<p>Depending on the project, an agent may need access to a terminal, package manager, test runner, build system, database tools, or version-control commands. The important question is what the agent should be allowed to do with each tool.</p>\n<h3>Practical Example: Updating an API Service</h3>\n<p>Suppose an AI coding agent is asked to modify an API endpoint. It may need to inspect the repository, edit implementation files, install an approved dependency, run tests, and create a commit. A controlled setup can permit these operations while preventing access to production credentials, infrastructure changes, or deployment commands.</p>\n<h3>Design Permissions Around Tasks</h3>\n<ul><li>Scoped to the work being performed</li><li>Limited to necessary resources</li><li>Auditable where appropriate</li><li>Reversible when access is no longer required</li></ul>\n<p>The objective is not to make AI coding agents ineffective through excessive restrictions. It is to provide the capabilities required for productive development while keeping higher-risk operations appropriately controlled.</p>\n<h2>Feedback Loops and Observability</h2>\n<p>AI coding agents often need more than a single attempt to complete a development task. They become more effective when the software environment provides useful signals about the results of their actions.</p>\n<p>Signals can come from application logs, runtime errors, test and build results, type-checking or linting output, performance checks, and code review findings.</p>\n<h3>Observability Makes Feedback Actionable</h3>\n<p>Feedback is most useful when it contains enough information to support diagnosis. A generic message such as &ldquo;request failed&rdquo; provides little direction. Structured logs, meaningful error messages, traces, and diagnostic outputs can reveal what happened, where it happened, and what conditions surrounded the failure.</p>\n<h3>Practical Example: Fixing an API Integration</h3>\n<p>Consider an AI coding agent modifying an API integration. The implementation passes its initial checks but fails when an external service returns an unexpected response. Useful diagnostic information can reveal the affected endpoint, response status, error condition, and related application behaviour. The agent can use those signals to investigate, adjust the implementation, and run the relevant checks again.</p>\n<p>A practical feedback cycle is: change, observe, diagnose, revise, check again.</p>\n<h2>Security and Guardrails for AI Coding Agents</h2>\n<p>As AI coding agents become capable of making broader changes and interacting with development systems, security becomes an important part of the engineering workflow. In harness engineering, guardrails define boundaries around actions that could create security, data, or operational risk &mdash; the same principles that govern <a href=\"/cyber-security-services-company\">secure software development</a> generally.</p>\n<ul><li>protecting credentials, API keys, and other secrets</li><li>isolating development environments from production systems</li><li>preventing unsafe or destructive commands</li><li>protecting sensitive files and data</li><li>requiring approval for high-impact operations</li><li>maintaining records of important agent actions</li></ul>\n<h3>Practical Example: Protecting Production Credentials</h3>\n<p>Consider an AI coding agent working on a backend service that connects to external APIs. The agent may need test credentials to complete development tasks, but production secrets should remain outside its normal working environment. An isolated setup can provide development credentials while keeping production authentication inaccessible.</p>\n<h3>Guardrails Should Reflect Risk</h3>\n<p>Not every AI-assisted action presents the same level of risk. Reading source code or running a local test is different from changing infrastructure, accessing sensitive data, or deploying an application. Low-risk development can carry greater automation; high-impact operations need stronger safeguards and human approval.</p>\n<p>The goal is not to prevent AI coding agents from performing useful work. It is to ensure that greater autonomy does not automatically result in greater authority.</p>\n<h2>Human Engineers and Decision Points</h2>\n<p>Harness engineering does not aim to remove engineers from the software development process. Instead, it helps determine where human judgment adds the most value.</p>\n<p>AI coding agents can handle implementation tasks efficiently, but some decisions require broader context than the immediate code change. These may involve product priorities, architectural direction, business rules, compliance requirements, or trade-offs that cannot be evaluated through automated checks alone.</p>\n<h3>Where Human Judgment Matters</h3>\n<ul><li>significant architectural changes</li><li>modifications to critical business logic</li><li>changes with security or compliance implications</li><li>unexpected behaviour discovered during validation</li><li>decisions involving long-term maintainability</li><li>changes that affect multiple systems or teams</li></ul>\n<p>The goal is not to manually review every action an AI coding agent performs &mdash; excessive intervention can reduce the productivity benefits of agentic development. Instead, teams can establish decision boundaries: routine, low-risk implementation can proceed with greater autonomy, while consequential decisions receive appropriate oversight.</p>\n<p>This creates a practical division of responsibility: AI coding agents handle implementation and iteration, automated systems validate measurable behaviour, and engineers make consequential decisions.</p>\n<h2>Common Harness Engineering Mistakes</h2>\n<p>Harness engineering can improve AI-assisted software development, but poorly designed workflows can create new problems. Many failures come not from the AI model itself, but from the way the surrounding development environment is designed.</p>\n<h3>1. Giving AI Too Much Context</h3>\n<p>More information does not always produce better results. Large amounts of outdated, irrelevant, or duplicated documentation can make it harder for an AI coding agent to identify what actually matters. <strong>Better approach:</strong> provide focused, current context that is relevant to the task.</p>\n<h3>2. Relying on Instructions Without Validation</h3>\n<p>Clear engineering rules can guide implementation, but instructions alone cannot prove that a change works. <strong>Better approach:</strong> connect guidance with automated tests, builds, type checks, and other measurable validation.</p>\n<h3>3. Automating High-Impact Decisions</h3>\n<p>Allowing an AI coding agent to make consequential architectural, infrastructure, or production decisions without appropriate review can increase operational risk. <strong>Better approach:</strong> define decision boundaries and require human involvement where the impact is significant.</p>\n<h3>4. Ignoring Failed Feedback</h3>\n<p>A failing test, build error, or runtime issue is valuable information. Treating it as an inconvenience rather than a signal can cause the same problem to repeat. <strong>Better approach:</strong> design workflows that allow the agent to inspect failures, revise its implementation, and validate the result again.</p>\n<h3>5. Treating the Harness as a One-Time Setup</h3>\n<p>Projects evolve. Architecture, dependencies, testing practices, and security requirements change over time. <strong>Better approach:</strong> continuously update repository context, instructions, validation, and controls as the software changes.</p>\n<h3>Practical Implementation Checklist</h3>\n<ul><li>Map the repository structure and major architectural boundaries</li><li>Document project-specific engineering instructions</li><li>Define which development tools the agent can use</li><li>Separate development and production environments</li><li>Protect secrets and sensitive data</li><li>Establish automated tests and validation checks</li><li>Define situations requiring human review</li><li>Capture useful build, test, and runtime feedback</li><li>Track agent actions where auditability is important</li><li>Review and update the harness as the project evolves</li></ul>\n<h2>How to Measure Harness Effectiveness</h2>\n<p>A harness should not be judged by how many rules, tools, or automated workflows it contains. Its value is determined by whether it helps teams deliver reliable software more effectively with AI coding agents.</p>\n<h3>Key Metrics to Track</h3>\n<ul><li><strong>Task completion rate</strong> &mdash; how often agents complete assigned tasks successfully.</li><li><strong>First-pass success rate</strong> &mdash; how frequently changes pass the expected validation without substantial rework.</li><li><strong>Rework rate</strong> &mdash; how often engineers need to significantly modify AI-generated implementations.</li><li><strong>Defect rate</strong> &mdash; whether AI-assisted changes introduce more defects or regressions.</li><li><strong>Cycle time</strong> &mdash; how long it takes to move a task from requirement to validated implementation.</li><li><strong>Review effort</strong> &mdash; how much engineering time is required to review and correct agent-generated changes.</li></ul>\n<p>These metrics should be considered together. A faster development cycle is not necessarily an improvement if it produces more defects or substantially increases review effort.</p>\n<h3>A Simple Measurement Process</h3>\n<ol><li><strong>Establish a baseline.</strong> Measure development time, rework, defects, and review effort before introducing or changing the harness.</li><li><strong>Track AI-assisted tasks.</strong> Record the same metrics for a defined set of tasks completed with AI coding agents.</li><li><strong>Compare outcomes.</strong> Look for changes in speed, quality, rework, and engineering effort rather than focusing on a single metric.</li><li><strong>Identify friction.</strong> Determine where agents repeatedly struggle &mdash; unclear requirements, missing repository context, failed validation, or excessive review.</li><li><strong>Improve and repeat.</strong> Adjust the relevant part of the harness and measure the next group of tasks.</li></ol>\n<p>The purpose of measurement is not simply to determine whether an AI coding agent is &ldquo;good&rdquo; or &ldquo;bad&rdquo;. It is to identify which parts of the engineering environment help or hinder effective AI-assisted development.</p>\n<h2>Practical Harness Engineering Implementation Roadmap</h2>\n<p>Teams do not need to redesign their entire development process before adopting harness engineering. A practical approach is to introduce AI coding agents gradually, starting with controlled tasks and expanding their role as the workflow matures. Organisations evaluating <a href=\"/generative-ai-development-company\">AI development services</a> can use the same sequence.</p>\n<ol><li><strong>Select suitable tasks.</strong> Start with tasks that have clear requirements and measurable outcomes &mdash; bug fixes, isolated features, refactoring, test creation, or documentation updates.</li><li><strong>Prepare the repository.</strong> Make the project structure, development commands, testing practices, and important architectural decisions easy to discover. Remove outdated or conflicting documentation.</li><li><strong>Define engineering guidance.</strong> Create concise instructions covering important coding patterns, architectural boundaries, testing expectations, and project-specific requirements.</li><li><strong>Connect validation.</strong> Ensure AI-generated changes can be evaluated through relevant tests, builds, type checks, linting, and other automated checks.</li><li><strong>Introduce controlled tool access.</strong> Provide the tools required for development tasks while restricting access to sensitive systems, credentials, and high-impact operations.</li><li><strong>Establish review boundaries.</strong> Define which tasks can proceed with greater autonomy and which require human review or approval.</li><li><strong>Measure and improve.</strong> Track completion rates, rework, defects, cycle time, and review effort. Use these results to identify weaknesses and improve the harness.</li></ol>\n<p>This incremental approach allows organisations to adopt agentic software development without treating autonomy as an all-or-nothing decision.</p>\n<h2>How mTouch Labs Approaches AI-Assisted Software Engineering</h2>\n<p>At <a href=\"/\">mTouch Labs</a>, AI-assisted software development is approached as part of a broader engineering strategy rather than as a standalone code-generation activity. The focus is on helping development teams use AI coding agents where they can create meaningful productivity gains while maintaining the engineering standards required for production software.</p>\n<h3>Applying AI Where It Adds Value</h3>\n<p>AI coding agents can support feature implementation, refactoring, test creation, debugging, documentation, and repetitive engineering tasks. The appropriate level of AI involvement depends on the nature of the project and the task.</p>\n<h3>Combining Automation With Engineering Expertise</h3>\n<p>AI can increase the speed of implementation, but experienced engineers remain important for understanding business requirements, evaluating technical trade-offs, reviewing critical changes, and maintaining the long-term direction of a software product.</p>\n<h3>Building for Real-World Delivery</h3>\n<p>For organisations adopting AI coding agents, the priority should be more than demonstrating that AI can write code. The real objective is a repeatable development workflow that produces maintainable, tested, secure, and production-ready software.</p>\n<h2>Conclusion</h2>\n<p>AI coding agents are changing how software can be designed, implemented, tested, and maintained. But their effectiveness depends on more than the capability of the underlying AI model.</p>\n<p>Harness Engineering provides the structure that allows AI to participate in software development within defined engineering expectations. By combining accessible project context, clear instructions, appropriate tools, automated validation, useful feedback, security safeguards, and human decision points, organisations can create a more reliable environment for AI-assisted development.</p>\n<p>The objective is not to give AI coding agents unlimited autonomy. It is to create the right boundaries so they can handle meaningful engineering work while important quality, security, and architectural decisions remain appropriately controlled.</p>\n<p>As AI coding agents become more capable, the organisations that benefit most may not simply be those using the most advanced models. They will be the ones that build effective engineering systems around those models. The future of AI-assisted software development is not just better AI &mdash; it is better engineering around AI.</p>",
  "image": "/images/blogs/harness-engineering-ai-coding-agents.webp",
  "author": "mTouch Labs",
  "category": "Software Development",
  "tags": [
    "Harness Engineering",
    "AI coding agents",
    "AI-assisted software development",
    "AI software engineering",
    "agentic software development",
    "AI engineering workflows"
  ],
  "status": "published",
  "published": true,
  "featured": false,
  "meta_title": "Harness Engineering: AI Coding Agents for Reliable Software",
  "meta_description": "Learn how Harness Engineering helps teams build reliable software with AI coding agents through context, testing, guardrails, automation, and human oversight.",
  "focus_keyword": "Harness Engineering",
  "secondary_keywords": "AI coding agents, AI-assisted software development, AI software engineering, agentic software development, AI engineering workflows",
  "canonical_url": "https://www.mtouchlabs.com/blog/harness-engineering-ai-coding-agents",
  "breadcrumb_title": "Harness Engineering",
  "og_title": "Harness Engineering: AI Coding Agents for Reliable Software",
  "og_description": "Learn how Harness Engineering helps teams build reliable software with AI coding agents through context, testing, guardrails, automation, and human oversight.",
  "faq_schema": [
    {
      "question": "What is Harness Engineering?",
      "answer": "Harness Engineering is an approach to designing the environment in which AI coding agents operate. It combines repository context, engineering instructions, development tools, validation, feedback mechanisms, security controls, and human oversight to help AI contribute reliably to software development."
    },
    {
      "question": "Why is Harness Engineering important for AI coding agents?",
      "answer": "AI coding agents can perform increasingly complex development tasks, but generated code still needs to fit the project's architecture, requirements, security standards, and quality expectations. Harness Engineering provides the surrounding structure needed to make AI-assisted development more controlled and repeatable."
    },
    {
      "question": "Is Harness Engineering the same as AI-assisted software development?",
      "answer": "No. AI-assisted software development broadly refers to using AI to support software engineering activities. Harness Engineering focuses specifically on designing the systems, workflows, rules, and validation mechanisms around AI coding agents so they can operate effectively within a software project."
    },
    {
      "question": "What should an AI engineering harness include?",
      "answer": "A practical harness can include repository context, engineering instructions, automated tests, development tools, feedback signals, security safeguards, controlled access, and defined points for human review. The exact configuration depends on the project's architecture and risk profile."
    },
    {
      "question": "How do you test AI-generated code?",
      "answer": "AI-generated changes can be evaluated using appropriate validation practices, including unit tests, integration tests, end-to-end tests, type checks, linting, builds, and application-level validation."
    },
    {
      "question": "Can AI coding agents work without human developers?",
      "answer": "AI coding agents can perform many implementation tasks with limited intervention, but human engineers remain important for requirements, architecture, technical trade-offs, security-sensitive decisions, and other high-impact areas. The objective is generally controlled autonomy rather than complete removal of human involvement."
    },
    {
      "question": "How do companies start implementing Harness Engineering?",
      "answer": "A practical starting point is to select well-defined development tasks, prepare repository context, establish engineering instructions, connect automated validation, introduce controlled tool access, define human review boundaries, and measure the results."
    },
    {
      "question": "What is the difference between an AI coding agent and a coding assistant?",
      "answer": "A coding assistant typically helps with activities such as code suggestions, explanations, or localized edits. An AI coding agent can operate across multiple development steps, such as inspecting a repository, modifying several files, running tools, evaluating results, and iterating on its implementation."
    },
    {
      "question": "Does Harness Engineering replace traditional software engineering practices?",
      "answer": "No. It extends traditional engineering practices to account for AI-driven development. Testing, code review, architecture, security, version control, documentation, and other established practices remain important when working with AI coding agents."
    },
    {
      "question": "What can mTouch Labs help with in AI-assisted software engineering?",
      "answer": "mTouch Labs can help businesses explore and implement AI-assisted software development across custom applications, engineering workflows, automation, testing, and modernization initiatives. Its approach combines AI capabilities with established software engineering practices to help teams improve development efficiency while maintaining quality, security, and maintainability."
    }
  ]
};

const client = new Client({ connectionString: url, ssl: { rejectUnauthorized: false } });

/* Postgres needs arrays and json in the shape the column expects, and the
   column types differ between installs (text[] vs jsonb vs plain text). */
function coerce(v, type) {
  if (v === null || v === undefined) return null;
  if (Array.isArray(v)) {
    if (type === 'ARRAY') return v;
    if (type === 'jsonb' || type === 'json') return JSON.stringify(v);
    return v.join(', ');
  }
  if (typeof v === 'object') return JSON.stringify(v);
  if (typeof v === 'boolean' && type !== 'boolean') return String(v);
  return v;
}

async function main() {
  await client.connect();
  const { rows: cols } = await client.query(
    `SELECT column_name, data_type FROM information_schema.columns WHERE table_name = $1`, [TABLE]);
  if (!cols.length) throw new Error(`Table "${TABLE}" not found.`);
  const typeOf = new Map(cols.map((c) => [c.column_name, c.data_type]));

  const keys = Object.keys(blog).filter((k) => typeOf.has(k));
  const missing = Object.keys(blog).filter((k) => !typeOf.has(k));
  if (missing.length) console.log(`  ! no such column, skipping: ${missing.join(', ')}`);

  /* These three are written as SQL NOW(), not as JS timestamps, for two
     reasons the /blog listing makes unavoidable:

       WHERE  ... AND (publish_date IS NULL OR publish_date <= NOW())
       ORDER BY GREATEST(updated_at, publish_date, created_at) DESC

     A publish_date even seconds ahead of the database clock is treated as
     SCHEDULED and the post stays hidden. Using the database's own clock
     removes any skew between this machine and the server, so the post is
     live the moment the insert commits. Writing all three also makes the
     GREATEST() the newest value in the table, which is what puts it at
     the top of the listing. */
  const NOW_COLS = ['publish_date', 'created_at', 'updated_at'].filter((c) => typeOf.has(c));
  const insertCols = [...keys, ...NOW_COLS.filter((c) => !keys.includes(c))];
  const values = keys.map((k) => coerce(blog[k], typeOf.get(k)));
  const quoted = insertCols.map((k) => `"${k}"`);
  const valueExprs = insertCols.map((k) =>
    NOW_COLS.includes(k) ? 'NOW()' : `$${keys.indexOf(k) + 1}`);
  const updates = insertCols
    .filter((k) => k !== 'slug')
    .map((k) => (NOW_COLS.includes(k) ? `"${k}" = NOW()` : `"${k}" = EXCLUDED."${k}"`));

  const sql = `INSERT INTO "${TABLE}" (${quoted.join(', ')})
    VALUES (${valueExprs.join(', ')})
    ON CONFLICT (slug) DO UPDATE SET ${updates.join(', ')}
    RETURNING slug, status, publish_date, (xmax = 0) AS inserted, LENGTH(content) AS chars`;

  if (DRY_RUN) {
    console.log(`  . would upsert ${blog.slug} with ${keys.length} columns, ${blog.content.length} chars of HTML`);
    await client.end(); return;
  }
  const { rows } = await client.query(sql, values);
  const r = rows[0];
  console.log(`  ${r.inserted ? 'INSERTED' : 'UPDATED '}  ${r.slug}  (${r.chars} chars)`);
  console.log(`      status: ${r.status}   publish_date: ${r.publish_date}`);
  console.log(`      live at /blog/${r.slug}`);

  /* Read the listing back with the EXACT query /blog/page.tsx runs, so the
     position is verified rather than assumed. */
  const { rows: top } = await client.query(`
    SELECT slug, publish_date
    FROM blogs
    WHERE (published = true OR status = 'published')
      AND (publish_date IS NULL OR publish_date <= NOW())
    ORDER BY GREATEST(updated_at, publish_date, created_at) DESC NULLS LAST
    LIMIT 5`);
  console.log('\n  /blog listing, top 5:');
  top.forEach((b, i) => console.log(`      ${i + 1}. ${b.slug}${b.slug === blog.slug ? '   <-- this post' : ''}`));
  console.log(top[0] && top[0].slug === blog.slug
    ? '\n  Confirmed: first in the listing.\n'
    : '\n  ! NOT first — another row has a newer updated_at/publish_date.\n');
  await client.end();
}

main().catch((e) => { console.error(`\n  Failed: ${e.message}\n`); process.exitCode = 1; client.end().catch(() => {}); });
